# NapCat 部署结构重设计 — 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 napcat/ 从 git 仓库中完全隔离，配置模板化到 config/napcat-templates/，消除 834 个临时目录污染

**Architecture:** `napcat/` 整体 gitignore（1 条规则替代 17 条）。`config/napcat-templates/` 存模板（git 追踪），`setup-napcat.ps1` 部署时复制到 `napcat/config/`。后端从 `config/qq-bot-config.json` 读取 napcat 路径。

**Tech Stack:** PowerShell 5.1 (setup-napcat.ps1), TypeScript 5.x (server.ts), Node.js fs-extra

**Spec:** `docs/superpowers/specs/2026-06-11-napcat-deployment-redesign.md`

> ⚠️ **Spec 勘误**: Spec 中 Step 3/6.4 的模板部署目标路径写为 `napcat/napcat/config/`，但 setup-napcat.ps1 会将 zip 中的 `napcat/` 子目录内容 flatten 到 napcat 根目录，所以实际配置路径是 `napcat/config/`。本计划使用修正后的路径。

---

## 文件变更总览

| 操作 | 文件 | 变更说明 |
|------|------|------|
| **新建** | `config/napcat-templates/onebot11.json` | OneBot WS 连接配置模板 |
| **新建** | `config/napcat-templates/napcat.json` | NapCat 运行时配置模板 |
| **新建** | `config/napcat-templates/webui.json` | WebUI 端口/token 模板 |
| **移动** | `qq-bot-config.json` → `config/qq-bot-config.json` | 加 `napcat` 字段，配置归位 |
| **修改** | `.gitignore` | 17 条 napcat 规则 → 1 条 `napcat/` |
| **修改** | `backend/src/server.ts` | config 路径、移除 BOM strip、可配置 napcat 路径 |
| **修改** | `scripts/setup-napcat.ps1` | 添加模板部署、简化 --Force、删除 ProtectedFiles |
| **修改** | `README.md` | 更新文件树中 qq-bot-config.json 路径 |
| **git** | `git rm -r --cached napcat/` | 停止追踪 napcat |

---

### Task 1: 创建配置模板目录和文件

**Files:**
- Create: `config/napcat-templates/onebot11.json`
- Create: `config/napcat-templates/napcat.json`
- Create: `config/napcat-templates/webui.json`

- [ ] **Step 1: 创建模板目录**

```bash
mkdir -p config/napcat-templates
```

- [ ] **Step 2: 写入 `config/napcat-templates/onebot11.json`**

```json
{
  "network": {
    "websocketClients": [
      {
        "name": "Snapshot Pi",
        "enable": true,
        "url": "ws://127.0.0.1:3001/qq/ws",
        "token": "",
        "messagePostFormat": "array",
        "reportSelfMessage": false,
        "reconnectInterval": 5000,
        "heartInterval": 30000
      }
    ]
  },
  "enableLocalFile2Url": true,
  "parseMultMsg": false
}
```

- [ ] **Step 3: 写入 `config/napcat-templates/napcat.json`**

```json
{
  "fileLog": true,
  "consoleLog": true,
  "fileLogLevel": "info",
  "consoleLogLevel": "info"
}
```

- [ ] **Step 4: 写入 `config/napcat-templates/webui.json`**

```json
{
  "host": "127.0.0.1",
  "port": 6099,
  "token": "",
  "loginRate": 3
}
```

- [ ] **Step 5: 验证文件均可解析为合法 JSON**

```bash
node -e "['onebot11','napcat','webui'].forEach(f => { const j = require('./config/napcat-templates/' + f + '.json'); console.log(f + ':', Object.keys(j).join(', ')); })"
```

Expected output:
```
onebot11: network, enableLocalFile2Url, parseMultMsg
napcat: fileLog, consoleLog, fileLogLevel, consoleLogLevel
webui: host, port, token, loginRate
```

- [ ] **Step 6: Commit**

```bash
git add config/napcat-templates/
git commit -m "feat: add NapCat config templates (onebot11, napcat, webui)

Templates live under config/napcat-templates/ (git-tracked).
setup-napcat.ps1 copies them to napcat/config/ at deploy time.
OneBot11 includes messagePostFormat, reconnectInterval, and heartInterval defaults.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 简化 .gitignore

**Files:**
- Modify: `.gitignore`

- [ ] **Step 1: 替换 .gitignore 中的 napcat 规则块**

定位 `.gitignore` 中第 27-50 行的 napcat 规则块（从 `# NapCat: large binaries` 到 `napcat.zip`），整体替换。

Old string (17 lines to remove):
```gitignore
# NapCat: large binaries (deployed by setup-napcat.ps1)
napcat/node.exe
napcat/napcat.mjs
napcat/wrapper.node
napcat/major.node
napcat/QQNT.dll
napcat/*.dll
napcat/node_modules/
napcat/native/
napcat/static/
napcat/worker/
napcat/plugins/

# NapCat: runtime-generated files
napcat/cache/
napcat/logs/
napcat/*.db*
napcat/*.log
napcat/config/napcat_*.json
napcat/config/onebot11_*.json
napcat/config/napcat_protocol_*.json

# NapCat: deployment leftovers
napcat.zip
```

New string:
```gitignore
# NapCat: entire directory auto-deployed by scripts/setup-napcat.ps1
# QQ NT runtime creates temp dirs in its working directory (TCD*.tmp,
# chrome_drag*, scoped_dir*, etc.) — normal behavior, not tracked.
napcat/
```

- [ ] **Step 2: 验证只有一条 napcat 规则**

```bash
grep -n "napcat" .gitignore
```

Expected: 一行输出 `XX:napcat/`（XX 是行号）

- [ ] **Step 3: 验证其他 ignore 规则未被误删**

```bash
grep -c "node_modules" .gitignore
```

Expected: 至少 1（node_modules/ 规则仍在）

- [ ] **Step 4: Commit**

```bash
git add .gitignore
git commit -m "refactor: simplify .gitignore — one napcat/ line replaces 17 fine-grained rules

The napcat/ directory is entirely auto-generated by setup-napcat.ps1.
Previously .gitignore tracked individual file types, but this missed
834+ temp dir patterns (TCD*.tmp, chrome_drag*, scoped_dir*, PSES-*,
{GUID} dirs, tmp-*, cloudcode-temp*, etc.) created by QQ NT's
Electron/Chromium runtime in its working directory.

Now a single 'napcat/' rule excludes everything — zero maintenance burden.
QQ NT temp dir creation is normal runtime behavior, not a bug to track.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: 移动并更新 qq-bot-config.json

**Files:**
- Move: `qq-bot-config.json` → `config/qq-bot-config.json`
- Modify: `config/qq-bot-config.json` (add `napcat` field, remove `qqPath`)

- [ ] **Step 1: 移动文件**

```bash
mv qq-bot-config.json config/qq-bot-config.json
```

- [ ] **Step 2: 更新内容 — 添加 `napcat` 块，移除 `qqPath`**

读取 `config/qq-bot-config.json`。当前内容以：
```json
{
  "enabled": true,
  "wsPath": "/qq/ws",
  ...
```

在 `"enabled"` 行后插入 `"napcat"` 配置块，同时移除 `"qqPath"` 字段（如存在）。

修改后：
```json
{
  "enabled": true,
  "napcat": {
    "path": "napcat/napcat.bat",
    "templateDir": "config/napcat-templates"
  },
  "wsPath": "/qq/ws",
  "accessToken": "",
  "dedicatedPresetId": "qq-tutor",
  "maxGroupContextMessages": 20,
  "rateLimit": {
    "maxMessages": 5,
    "windowSeconds": 10
  },
  "triggerKeywords": ["@bot", "/ai", "/ask"],
  "quiz": {
    "enabled": true,
    "questionsPerRound": 3,
    "xpPerGrade": { "0": 0, "1": 1, "2": 3, "3": 5, "4": 10 }
  },
  "rendering": {
    "formulaImageWidth": 800,
    "maxMessageLength": 1500,
    "messageChunkOverlap": 100
  },
  "groupSync": {
    "enabled": true,
    "allowedGroupIds": []
  }
}
```

- [ ] **Step 3: 验证 JSON 结构和新增字段**

```bash
node -e "const j = require('./config/qq-bot-config.json'); console.log('napcat.path:', j.napcat?.path); console.log('napcat.templateDir:', j.napcat?.templateDir); console.log('enabled:', j.enabled); console.log('wsPath:', j.wsPath);"
```

Expected:
```
napcat.path: napcat/napcat.bat
napcat.templateDir: config/napcat-templates
enabled: true
wsPath: /qq/ws
```

- [ ] **Step 4: 确认旧路径的文件已删除**

```bash
test -f qq-bot-config.json && echo "OLD FILE STILL EXISTS!" || echo "OK: old file removed"
```

Expected: `OK: old file removed`

- [ ] **Step 5: Commit**

```bash
git add config/qq-bot-config.json
git rm qq-bot-config.json
git commit -m "refactor: move qq-bot-config.json to config/, add napcat config block

- Config file now lives in config/ alongside napcat-templates/
- New 'napcat.path' field: path to napcat.bat (replaces hardcoded path)
- New 'napcat.templateDir': points to config/napcat-templates/
- Removed obsolete 'qqPath' field

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: 更新 backend/src/server.ts

**Files:**
- Modify: `backend/src/server.ts:308`
- Modify: `backend/src/server.ts:353-360` (remove BOM strip block)
- Modify: `backend/src/server.ts:373-375` (configurable napcat path + existence check)
- Modify: `backend/src/server.ts:464` (error message path)

- [ ] **Step 1: 更新 qq-bot-config.json 读取路径**

Change line 308:
```typescript
// Before
const qqConfigPath = path.join(workspaceCwd, 'qq-bot-config.json');

// After
const qqConfigPath = path.join(workspaceCwd, 'config', 'qq-bot-config.json');
```

- [ ] **Step 2: 删除 preflightNapCat 中的 BOM strip 逻辑**

Remove lines 353–360 (the entire BOM strip block):

```typescript
// DELETE the following block:
    // Strip BOM from JSON files (known issue: NapCat JSONs with UTF-8 BOM crash on parse)
    const stripBom = path.join(workspaceCwd, 'scripts', 'strip-bom.js');
    if (await fs.pathExists(stripBom)) {
      try {
        const { execSync } = await import('child_process');
        execSync(`node "${stripBom}"`, { cwd: dir, timeout: 5000, windowsHide: true });
      } catch { /* strip-bom failure is non-blocking */ }
    }
```

替换为注释：
```typescript
    // Note: BOM cleanup no longer needed — config templates are clean UTF-8 without BOM.
    // strip-bom.js is kept under scripts/ for other use cases.
```

- [ ] **Step 3: 更新 preflightNapCat 中的配置检查路径**

Change the onebot11 config check path to use the correct target path (after zip flattening):

```typescript
// Before
{ file: path.join('config', 'onebot11.json'), label: 'OneBot config' },

// After
{ file: path.join('napcat', 'config', 'onebot11.json'), label: 'OneBot config' },
```

Wait, no. Let me re-read the code. `preflightNapCat` checks relative to `dir`:
```typescript
const dir = path.join(workspaceCwd, 'napcat');
...
if (!await fs.pathExists(path.join(dir, c.file))) {
```

Current check: `path.join(dir, 'config', 'onebot11.json')` = `napcat/config/onebot11.json`. This is actually already correct — after zip flattening, the config lands at `napcat/config/onebot11.json`. So **no change needed** here. Skip this step.

- [ ] **Step 4: Make spawnNapCat path configurable and add existence check**

Replace lines 373–379 in `spawnNapCat()`:

```typescript
// Before
function spawnNapCat(): ChildProcess {
    const napcatDir = path.join(workspaceCwd, 'napcat');
    const proc = spawn('napcat.bat', [], {
      cwd: napcatDir,
      shell: true,
      stdio: 'pipe',
    });
```

With:
```typescript
function spawnNapCat(): ChildProcess {
    const napcatScriptRel = qqConfig?.napcat?.path || 'napcat/napcat.bat';
    const napcatScript = path.join(workspaceCwd, napcatScriptRel);

    if (!fs.existsSync(napcatScript)) {
      const error = new Error(
        `NapCat 启动脚本不存在: ${napcatScriptRel}\n` +
        `请先运行部署: scripts\\setup.bat`
      );
      console.error(`[QQ] ${error.message}`);
      throw error;
    }

    const napcatDir = path.dirname(napcatScript);
    const proc = spawn(napcatScript, [], {
      cwd: napcatDir,
      shell: true,
      stdio: 'pipe',
    });
```

- [ ] **Step 5: 更新错误消息中的路径引用**

Change line 464:
```typescript
// Before
return res.status(400).json({ success: false, error: '未找到 qq-bot-config.json 配置文件' });

// After
return res.status(400).json({ success: false, error: '未找到 config/qq-bot-config.json 配置文件' });
```

- [ ] **Step 6: 验证 TypeScript 编译**

```bash
cd backend && npx tsc --noEmit src/server.ts 2>&1 | head -20
```

Expected: no errors (或仅 pre-existing errors，没有由本次修改引入的新错误)

- [ ] **Step 7: Commit**

```bash
git add backend/src/server.ts
git commit -m "refactor(server): configurable napcat path, remove BOM strip on startup

- Read qq-bot-config.json from config/ (not project root)
- Read napcat.path from config instead of hardcoding 'napcat/napcat.bat'
- Add existence check before spawning napcat.bat (clear error + hint)
- Remove BOM strip block in preflightNapCat (config templates are clean UTF-8)
- Update error message path references

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: 重构 scripts/setup-napcat.ps1

**Files:**
- Modify: `scripts/setup-napcat.ps1`

This is the largest change. We need to:
1. Remove `$ProtectedFiles` array
2. Add `Deploy-NapCatConfigTemplates` function
3. Simplify `Install-NapCatShell` — remove backup/restore, remove ProtectedFiles skip
4. Simplify `Clear-UnnecessaryFiles` — remove root-level napcat.mjs filter (no longer needed with --Force behavior)
5. Update `Test-Deployment` — check correct config path
6. Add config template deployment step to Main
7. Update SYNOPSIS to reflect new config template approach

- [ ] **Step 1: Remove `$ProtectedFiles` array (lines 33-40)**

Delete:
```powershell
# ═══════════════════════════════════════════════════════════════════════════
# Protected files (git-tracked custom files — never overwritten by upstream)
# ═══════════════════════════════════════════════════════════════════════════
$ProtectedFiles = @(
    'napcat.bat',
    'index.cjs',
    'config.json',
    'package.json',
    'qqnt.json',
    'KillQQ.bat'
)
```

- [ ] **Step 2: Remove ProtectedFiles skip logic in Install-NapCatShell (lines 135-139)**

Delete:
```powershell
        # Skip protected files
        if ($ProtectedFiles -contains $item.Name) {
            Write-Host "    [skip protected] $($item.Name)" -ForegroundColor DarkGray
            continue
        }
```

- [ ] **Step 3: Remove onebot11 backup/restore logic in Install-NapCatShell (lines 122-127 and 173-176)**

Delete the backup block:
```powershell
    # Backup user config
    $onebotBackup = $null
    $onebotConfigPath = Join-Path $NapCatDir 'config\onebot11.json'
    if (Test-Path $onebotConfigPath) {
        $onebotBackup = Get-Content $onebotConfigPath -Raw -Encoding UTF8
    }
```

Delete the restore block:
```powershell
    # Restore user config
    if ($onebotBackup) {
        $onebotBackup | Set-Content $onebotConfigPath -Encoding UTF8 -NoNewline
        Write-Host "  Restored onebot11.json" -ForegroundColor Green
    }
```

Also update `$onebotConfigPath` reference — keep the variable but repurpose it under the new function.

- [ ] **Step 4: Remove root-level napcat.mjs filter (the one with PSIsContainer check)**

Lines 147-150 — delete the root-level napcat.mjs filter:
```powershell
        # Skip root-level napcat.mjs (the real one is in napcat/ subdirectory)
        if ($item.Name -eq 'napcat.mjs' -and (-not $item.PSIsContainer)) {
            Write-Host "    [skip root-level] napcat.mjs" -ForegroundColor DarkGray
            continue
        }
```

With the new `--Force` design (clear entire `napcat/`), this filtering is no longer needed.

- [ ] **Step 5: Add `Deploy-NapCatConfigTemplates` function**

Insert before the Test-Deployment function (after Clear-UnnecessaryFiles):

```powershell
# ═══════════════════════════════════════════════════════════════════════════
# Step 3.5: Deploy config templates from git-tracked config/napcat-templates/
# ═══════════════════════════════════════════════════════════════════════════
function Deploy-NapCatConfigTemplates {
    Write-Step 4 5 'Deploying config templates...' 'Cyan'

    $TemplateDir = Join-Path $RootDir 'config\napcat-templates'

    if (-not (Test-Path $TemplateDir)) {
        Write-Host "  ERROR: Template directory not found: $TemplateDir" -ForegroundColor Red
        Write-Host "  This should never happen — config/napcat-templates/ is git-tracked." -ForegroundColor Red
        exit 1
    }

    # Config files land at napcat/config/ (after zip napcat/ subdir flattening)
    $TargetDir = Join-Path $NapCatDir 'config'
    New-Item -ItemType Directory -Path $TargetDir -Force | Out-Null

    $templates = @('onebot11.json', 'napcat.json', 'webui.json')
    foreach ($t in $templates) {
        $src = Join-Path $TemplateDir $t
        if (-not (Test-Path $src)) {
            Write-Host "  ERROR: Template missing: $t" -ForegroundColor Red
            Write-Host "  Expected at: $src" -ForegroundColor Red
            exit 1
        }

        # Verify valid JSON before copying
        try {
            Get-Content $src -Raw -Encoding UTF8 | ConvertFrom-Json | Out-Null
        } catch {
            Write-Host "  ERROR: Template is not valid JSON: $t" -ForegroundColor Red
            Write-Host "  $_" -ForegroundColor Red
            exit 1
        }

        Copy-Item $src $TargetDir -Force
        Write-Host "  [OK] $t -> napcat/config/" -ForegroundColor Green
    }

    # Verify the deployed onebot11.json structure
    $deployedConfig = Join-Path $TargetDir 'onebot11.json'
    try {
        $config = Get-Content $deployedConfig -Raw -Encoding UTF8 | ConvertFrom-Json
        $wsClients = $config.network.websocketClients
        if ($wsClients -and $wsClients.Count -gt 0) {
            foreach ($client in $wsClients) {
                if ($client.url -and $client.enable) {
                    Write-Host "  [OK] WS: $($client.name) -> $($client.url)" -ForegroundColor Green
                }
            }
        } else {
            Write-Host "  [WARN] No WebSocket clients configured in onebot11.json" -ForegroundColor Yellow
        }
    } catch {
        Write-Host "  [ERR] Deployed onebot11.json validation failed: $_" -ForegroundColor Red
        exit 1
    }
}
```

- [ ] **Step 6: Update `Test-Deployment` config path**

Change the onebot11.json path in the `$required` array (line 447):
```powershell
# Before
@{Path='config\onebot11.json'; Label='OneBot config'; Critical=$true},

# After
@{Path='config\onebot11.json';  Label='OneBot config (deployed from config/napcat-templates/)'; Critical=$true},
```

And update the validation section to use the correct path:
```powershell
    # Validate onebot11.json content
    $configPath = Join-Path $NapCatDir 'config\onebot11.json'
```

(This is already correct — just verify it matches)

- [ ] **Step 7: Update Main to add template deployment step**

Add after `Clear-UnnecessaryFiles` and before `Test-Deployment` in the try block (line ~513):

```powershell
try {
    Install-NapCatShell
    Sync-QQNTBinaries
    Clear-UnnecessaryFiles
    Deploy-NapCatConfigTemplates   # <-- NEW
    $ok = Test-Deployment
```

- [ ] **Step 8: Update comment with total step count**

In `Write-Step` calls, update the total:
- `Install-NapCatShell`: `Write-Step 1 5 'Downloading NapCat Shell...'` (was 1 4)
- `Sync-QQNTBinaries`: `Write-Step 2 5 'Extracting QQNT binaries...'` (was 2 4)
- `Clear-UnnecessaryFiles`: `Write-Step 3 5 'Cleaning up unnecessary files...'` (was 3 4)
- `Deploy-NapCatConfigTemplates`: uses `Write-Step 4 5` (new)
- `Test-Deployment`: `Write-Step 5 5 'Verifying deployment...'` (was 4 4)

- [ ] **Step 9: Update SYNOPSIS and Main header**

Change SYNOPSIS (lines 1-17):

```powershell
<#
.SYNOPSIS
    NapCat QQ Shell self-contained deployment (zero-dependency)
.DESCRIPTION
    1. Download NapCat.Shell.zip (no QQNT desktop needed)
    2. Silent-extract wrapper.node + DLLs from QQNT NSIS installer
    3. Clean up unnecessary files + deploy config templates
    4. Verify deployment integrity
    5. napcat/ is fully self-contained after deployment
    Config templates live in config/napcat-templates/ (git-tracked) and
    are copied to napcat/config/ at deploy time.
.PARAMETER Force
    Force re-download and re-extract all components
.PARAMETER NapCatVersion
    NapCat Shell version (default v4.18.4)
.EXAMPLE
    .\setup-napcat.ps1
    .\setup-napcat.ps1 -Force
    .\setup-napcat.ps1 -NapCatVersion "v4.20.0"
#>
```

Update Main header text (line 496):
```powershell
Write-Host "  NapCat QQ Shell - Self-Contained Setup (5 steps)" -ForegroundColor Cyan
```

- [ ] **Step 10: Verify PowerShell syntax**

```bash
powershell -NoProfile -Command "Get-Command 'scripts/setup-napcat.ps1' -Syntax"
```

- [ ] **Step 11: Commit**

```bash
git add scripts/setup-napcat.ps1
git commit -m "refactor(setup-napcat): add template deployment, simplify ProtectedFiles

- Remove \$ProtectedFiles array — config templates are in git, not napcat/
- Remove onebot11 backup/restore logic — templates in git replace this
- Remove root-level napcat.mjs filter — no longer needed
- Add Deploy-NapCatConfigTemplates function (step 4 of 5)
  - Copies config/napcat-templates/*.json -> napcat/config/
  - Validates JSON structure before and after copy
  - Verifies WebSocket client config in deployed onebot11.json
- Update step numbering from 4 to 5 steps
- Update SYNOPSIS and header text

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 从 git 索引中移除 napcat/

**Files:**
- git index only (working tree unchanged)

- [ ] **Step 1: 确认 napcat/ 已加入 .gitignore**

```bash
grep "^napcat/" .gitignore
```

Expected: `napcat/`

- [ ] **Step 2: 从 git 索引移除 napcat/（保留工作目录文件）**

```bash
git rm -r --cached napcat/ 2>&1
```

Expected: 列出所有被 untrack 的 napcat 文件（可能很长，几百行输出）

- [ ] **Step 3: 验证 git status — napcat 不再出现**

```bash
git status --short | grep napcat
```

Expected: 没有输出（napcat/ 不再被 git 追踪，且 .gitignore 生效）

- [ ] **Step 4: 验证 napcat/ 文件仍存在于磁盘**

```bash
ls napcat/napcat.bat 2>/dev/null && echo "napcat.bat exists on disk (as expected)" || echo "WARNING: napcat.bat missing"
```

Expected: `napcat.bat exists on disk (as expected)`

- [ ] **Step 5: Commit**

```bash
git add .gitignore
git commit -m "chore: stop tracking napcat/ entirely (moved to .gitignore)

git rm -r --cached napcat/ removes all napcat files from the index.
The working directory files are preserved. .gitignore's 'napcat/' rule
now excludes them from future commits.

This eliminates the 834+ temp directory pollution problem where QQ NT's
Electron/Chromium runtime creates temp dirs in its working directory.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: 更新 README.md

**Files:**
- Modify: `README.md` (line 85)

- [ ] **Step 1: 更新文件树中的配置路径**

Change line 85:
```markdown
# Before
├── qq-bot-config.json                # QQ Bot 运行时配置 (关键词/限流/渲染/测验)

# After
├── config/
│   ├── qq-bot-config.json            # QQ Bot 运行时配置 (关键词/限流/渲染/测验)
│   └── napcat-templates/             # NapCat 配置模板 (部署时复制到 napcat/config/)
```

- [ ] **Step 2: 更新配置说明部分**

Change line 244:
```markdown
# Before
`qq-bot-config.json` 控制运行参数：

# After
`config/qq-bot-config.json` 控制运行参数：
```

- [ ] **Step 3: Commit**

```bash
git add README.md
git commit -m "docs: update README for new config paths and napcat/ gitignore

- qq-bot-config.json moved to config/
- Add napcat-templates/ to file tree
- Update config reference paths

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: 端到端验证

- [ ] **Step 1: 验证 git status 完全干净（不含 napcat）**

```bash
git status
```

Expected: "working tree clean" 或只显示 napcat 外的未追踪文件（且 napcat/ 不在列表中）

- [ ] **Step 2: 模拟全新 clone 验证 — 确认 napcat 模板和脚本就位**

```bash
echo "=== git-tracked config files ===" && git ls-files config/ && echo "=== git-tracked scripts ===" && git ls-files scripts/setup-napcat.ps1
```

Expected:
```
config/napcat-templates/onebot11.json
config/napcat-templates/napcat.json
config/napcat-templates/webui.json
config/qq-bot-config.json
scripts/setup-napcat.ps1
```

- [ ] **Step 3: 验证 napcat/ 不在 git 追踪中**

```bash
git ls-files napcat/
```

Expected: 没有输出（空）

- [ ] **Step 4: 验证 .gitignore 规则顺序正确**

```bash
cat .gitignore
```

Expected: `napcat/` 在文件末尾附近，前面是有效的规则

- [ ] **Step 5: 运行 TypeScript 编译检查**

```bash
cd backend && npx tsc --noEmit 2>&1 | tail -5
```

Expected: 无新增编译错误（可接受 pre-existing errors）

- [ ] **Step 6: Final commit（如有多余变更）**

```bash
git status
git add -A  # only if there are remaining related changes
git commit -m "chore: finalize napcat deployment redesign migration"
```

---

## 验证清单

实现完成后逐项确认：

- [ ] `config/napcat-templates/` 下 3 个 JSON 文件存在且内容正确
- [ ] `config/qq-bot-config.json` 包含 `napcat.path` 和 `napcat.templateDir` 字段
- [ ] `.gitignore` 只有一条 `napcat/` 规则
- [ ] `git ls-files napcat/` 无输出（napcat 已从索引移除）
- [ ] `git status` 不显示 napcat/ 下任何文件
- [ ] `backend/src/server.ts` 读取 `config/qq-bot-config.json`（不是根目录）
- [ ] `backend/src/server.ts` 没有 `strip-bom.js` 调用
- [ ] `spawnNapCat()` 使用 `qqConfig.napcat.path` 而非硬编码路径
- [ ] `setup-napcat.ps1` 有 5 个步骤（含模板部署）
- [ ] `setup-napcat.ps1` 没有 `$ProtectedFiles` 引用
- [ ] `setup-napcat.ps1` 没有 `onebot11` 备份/恢复逻辑
- [ ] `README.md` 文件树反映新路径
